openapi: 3.1.0
info:
  title: OpenIM Intelligent Collaboration Platform API
  version: 0.1.0
paths:
  /healthz:
    get:
      operationId: getHealth
      responses:
        "200":
          description: Process is ready to accept requests.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
  /v1/im/session:
    post:
      operationId: createIMSession
      security:
        - oidcBearer: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateIMSessionRequest"
      responses:
        "200":
          description: OpenIM session material for the authenticated member.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateIMSessionResponse"
        "400":
          $ref: "#/components/responses/ErrorResponse"
        "401":
          $ref: "#/components/responses/ErrorResponse"
        "403":
          $ref: "#/components/responses/ErrorResponse"
        "409":
          $ref: "#/components/responses/ErrorResponse"
        "502":
          $ref: "#/components/responses/ErrorResponse"
        "500":
          $ref: "#/components/responses/ErrorResponse"
  /v1/im/devices:
    get:
      operationId: listMemberDevices
      security:
        - oidcBearer: []
      parameters:
        - in: query
          name: platform_id
          required: true
          schema:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
        - in: query
          name: device_id
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Safe member-owned enrollment and platform-online projection.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceSnapshot"
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
        "500": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/im/platforms/{platform_id}/logout:
    post:
      operationId: logoutMemberPlatform
      security:
        - oidcBearer: []
      parameters:
        - in: path
          name: platform_id
          required: true
          description: Another enrolled OpenIM platform to log out.
          schema:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogoutPlatformRequest"
      responses:
        "200":
          description: The target platform has been logged out by OpenIM.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LogoutPlatformResponse"
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
        "500": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/agent/intents/{intent_id}/approve:
    post:
      operationId: approveAgentIntent
      security:
        - oidcBearer: []
      parameters:
        - in: path
          name: intent_id
          required: true
          schema:
            type: string
            minLength: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApproveIntentRequest"
      responses:
        "202":
          description: Approval was recorded and execution was queued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApproveIntentResponse"
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
        "500": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/agent/workspace:
    get:
      operationId: getAgentWorkspace
      security:
        - oidcBearer: []
      parameters:
        - in: query
          name: platform_id
          required: true
          schema:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
        - in: query
          name: device_id
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Authenticated member Agent workspace projection.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentWorkspace"
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/agents:
    get:
      operationId: listAgents
      security:
        - oidcBearer: []
      parameters:
        - in: query
          name: platform_id
          required: true
          schema:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
        - in: query
          name: device_id
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Active tenant Agent definitions and their pinned production release metadata.
          content:
            application/json:
              schema:
                type: array
                maxItems: 50
                items: { "$ref": "#/components/schemas/AgentSummary" }
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/agent/channels/telegram/link:
    get:
      operationId: getTelegramLinkStatus
      security:
        - oidcBearer: []
      parameters:
        - in: query
          name: platform_id
          required: true
          schema:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
        - in: query
          name: device_id
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "200":
          description: Member-owned Telegram binding status without raw Telegram identifiers.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/TelegramLinkStatus" }
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "503": { "$ref": "#/components/responses/ErrorResponse" }
        "500": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/agent/channels/telegram/link-challenges:
    post:
      operationId: issueTelegramLinkChallenge
      description: Creates a challenge with no request body; tenant and member identity come only from the authenticated device context.
      security:
        - oidcBearer: []
      parameters:
        - in: query
          name: platform_id
          required: true
          schema:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
        - in: query
          name: device_id
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 128
      responses:
        "201":
          description: A new one-time Telegram link challenge. Plaintext is returned only in this response.
          headers:
            Cache-Control:
              schema: { type: string, const: no-store }
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/TelegramLinkChallenge" }
        "400": { "$ref": "#/components/responses/ErrorResponse" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
        "429": { "$ref": "#/components/responses/ErrorResponse" }
        "503": { "$ref": "#/components/responses/ErrorResponse" }
        "500": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/knowledge/documents:
    get:
      operationId: getKnowledgeAdminSnapshot
      security:
        - oidcBearer: []
      parameters:
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
        - { in: query, name: document_id, required: false, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Knowledge administration snapshot for an authorized administrator.
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/KnowledgeAdminSnapshot" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
    post:
      operationId: uploadKnowledgeDocument
      security:
        - oidcBearer: []
      parameters:
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, minLength: 16, maxLength: 200 } }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema: { "$ref": "#/components/schemas/KnowledgeUpload" }
      responses:
        "202":
          description: Immutable source stored and ingestion durably queued.
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/KnowledgeUploadResult" }
        "200":
          description: Idempotent replay of an existing immutable version.
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/KnowledgeUploadResult" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
        "422": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/knowledge/documents/{document_id}/versions:
    get:
      operationId: listKnowledgeDocumentVersions
      security:
        - oidcBearer: []
      parameters:
        - { in: path, name: document_id, required: true, schema: { type: string, format: uuid } }
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
      responses:
        "200":
          description: Immutable versions ordered newest first.
          content:
            application/json:
              schema:
                type: array
                items: { "$ref": "#/components/schemas/KnowledgeVersion" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "404": { "$ref": "#/components/responses/ErrorResponse" }
    post:
      operationId: uploadKnowledgeDocumentVersion
      security:
        - oidcBearer: []
      parameters:
        - { in: path, name: document_id, required: true, schema: { type: string, format: uuid } }
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
        - { in: header, name: Idempotency-Key, required: true, schema: { type: string, minLength: 16, maxLength: 200 } }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema: { "$ref": "#/components/schemas/KnowledgeUpload" }
      responses:
        "202":
          description: New immutable version stored and durably queued.
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/KnowledgeUploadResult" }
        "200":
          description: Idempotent replay.
          content:
            application/json:
              schema: { "$ref": "#/components/schemas/KnowledgeUploadResult" }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
        "422": { "$ref": "#/components/responses/ErrorResponse" }
        "502": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/knowledge/documents/{document_id}/versions/{version_id}/publish:
    post:
      operationId: publishKnowledgeDocumentVersion
      security:
        - oidcBearer: []
      parameters:
        - { in: path, name: document_id, required: true, schema: { type: string, format: uuid } }
        - { in: path, name: version_id, required: true, schema: { type: string, format: uuid } }
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
      responses:
        "200": { description: Version became the only current published version. }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "404": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/knowledge/documents/{document_id}/unpublish:
    post:
      operationId: unpublishKnowledgeDocument
      security:
        - oidcBearer: []
      parameters:
        - { in: path, name: document_id, required: true, schema: { type: string, format: uuid } }
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
      responses:
        "200": { description: Document was removed from production retrieval. }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "404": { "$ref": "#/components/responses/ErrorResponse" }
        "409": { "$ref": "#/components/responses/ErrorResponse" }
  /v1/knowledge/documents/{document_id}/grants/{member_id}:
    put:
      operationId: grantKnowledgeDocumentRead
      security:
        - oidcBearer: []
      parameters:
        - { in: path, name: document_id, required: true, schema: { type: string, format: uuid } }
        - { in: path, name: member_id, required: true, schema: { type: string, format: uuid } }
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
      responses:
        "200": { description: Direct read grant is active. }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "404": { "$ref": "#/components/responses/ErrorResponse" }
    delete:
      operationId: revokeKnowledgeDocumentRead
      security:
        - oidcBearer: []
      parameters:
        - { in: path, name: document_id, required: true, schema: { type: string, format: uuid } }
        - { in: path, name: member_id, required: true, schema: { type: string, format: uuid } }
        - { in: query, name: platform_id, required: true, schema: { type: integer, const: 5 } }
        - { in: query, name: device_id, required: true, schema: { type: string, minLength: 1, maxLength: 128 } }
      responses:
        "200": { description: Direct read grant was revoked immediately. }
        "401": { "$ref": "#/components/responses/ErrorResponse" }
        "403": { "$ref": "#/components/responses/ErrorResponse" }
        "404": { "$ref": "#/components/responses/ErrorResponse" }
components:
  securitySchemes:
    oidcBearer:
      type: http
      scheme: bearer
      bearerFormat: JWT
  responses:
    ErrorResponse:
      description: Typed request failure.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    HealthResponse:
      type: object
      additionalProperties: false
      required: [status, service, version]
      properties:
        status:
          type: string
          const: ready
        service:
          type: string
          const: platform-api
        version:
          type: string
          minLength: 1
    CreateIMSessionRequest:
      type: object
      additionalProperties: false
      required: [platform_id, device_id]
      properties:
        platform_id:
          type: integer
          minimum: 1
          maximum: 11
          not:
            const: 10
        device_id:
          type: string
          minLength: 1
          maxLength: 128
    CreateIMSessionResponse:
      type: object
      additionalProperties: false
      required: [user_id, ws_url, user_token, expires_at]
      properties:
        user_id:
          type: string
          minLength: 1
        ws_url:
          type: string
          format: uri
        user_token:
          type: string
          minLength: 1
        expires_at:
          type: string
          format: date-time
    DeviceSnapshot:
      type: object
      additionalProperties: false
      required: [devices, online_platform_ids]
      properties:
        devices:
          type: array
          items: { "$ref": "#/components/schemas/MemberDevice" }
        online_platform_ids:
          type: array
          uniqueItems: true
          items:
            type: integer
            minimum: 1
            maximum: 11
            not: { const: 10 }
    MemberDevice:
      type: object
      additionalProperties: false
      required: [device_id, platform_id, status, created_at, updated_at, current, online]
      properties:
        device_id: { type: string, minLength: 1, maxLength: 128 }
        platform_id:
          type: integer
          minimum: 1
          maximum: 11
          not: { const: 10 }
        status: { type: string, enum: [active, disabled] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        current: { type: boolean }
        online:
          type: boolean
          description: Platform-level OpenIM online state, not per-device activity.
    LogoutPlatformRequest:
      type: object
      additionalProperties: false
      required: [current_platform_id, current_device_id]
      properties:
        current_platform_id:
          type: integer
          minimum: 1
          maximum: 11
          not: { const: 10 }
        current_device_id: { type: string, minLength: 1, maxLength: 128 }
    LogoutPlatformResponse:
      type: object
      additionalProperties: false
      required: [platform_id, state, correlation_id]
      properties:
        platform_id:
          type: integer
          minimum: 1
          maximum: 11
          not: { const: 10 }
        state: { type: string, const: logged_out }
        correlation_id: { type: string, minLength: 1 }
    ApproveIntentRequest:
      type: object
      additionalProperties: false
      required: [platform_id, device_id, payload_digest]
      properties:
        platform_id:
          type: integer
          minimum: 1
          maximum: 11
          not: { const: 10 }
        device_id:
          type: string
          minLength: 1
          maxLength: 128
        payload_digest:
          type: string
          pattern: "^sha256:[0-9a-f]{64}$"
    ApproveIntentResponse:
      type: object
      additionalProperties: false
      required: [intent_id, execution_id, state]
      properties:
        intent_id: { type: string, minLength: 1 }
        execution_id: { type: string, minLength: 1 }
        state: { type: string, enum: [queued, running, unknown, succeeded, failed] }
    AgentWorkspace:
      type: object
      additionalProperties: false
      required: [agent_user_id, runs]
      properties:
        agent_user_id: { type: string, minLength: 1 }
        runs:
          type: array
          maxItems: 30
          items: { "$ref": "#/components/schemas/AgentRun" }
    AgentSummary:
      type: object
      additionalProperties: false
      required: [agent_id, slug, display_name, description, trigger_alias, production_version_number, production_version_id, spec_checksum, bot_user_id]
      properties:
        agent_id: { type: string, minLength: 1 }
        slug: { type: string, pattern: "^[a-z][a-z0-9-]{0,62}$" }
        display_name: { type: string, minLength: 1, maxLength: 120 }
        description: { type: string, maxLength: 500 }
        trigger_alias: { type: string, pattern: "^@[a-z][a-z0-9_-]{0,62}$" }
        production_version_number: { type: integer, minimum: 1 }
        production_version_id: { type: string, minLength: 1 }
        spec_checksum: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        bot_user_id: { type: string, minLength: 1 }
    AgentRun:
      type: object
      additionalProperties: false
      required: [run_id, agent_id, agent_display_name, agent_version_number, agent_spec_checksum, conversation_id, prompt, state, created_at, updated_at, citations]
      properties:
        run_id: { type: string, minLength: 1 }
        agent_id: { type: string, minLength: 1 }
        agent_display_name: { type: string, minLength: 1, maxLength: 120 }
        agent_version_number: { type: integer, minimum: 1 }
        agent_spec_checksum: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        conversation_id: { type: string, minLength: 1 }
        prompt: { type: string }
        state: { type: string, enum: [queued, running, reply_pending, waiting_approval, succeeded, failed] }
        answer: { type: string }
        model: { type: string }
        last_error: { type: string }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
        citations:
          type: array
          items: { "$ref": "#/components/schemas/AgentCitation" }
        intent: { "$ref": "#/components/schemas/AgentIntent" }
    AgentCitation:
      type: object
      additionalProperties: false
      required: [citation_id, title, source_uri, checksum]
      properties:
        citation_id: { type: string, minLength: 1 }
        title: { type: string, minLength: 1 }
        source_uri: { type: string, minLength: 1 }
        checksum: { type: string, minLength: 1 }
        authorized_excerpt: { type: string, minLength: 1, maxLength: 1200 }
    KnowledgeUpload:
      type: object
      additionalProperties: false
      required: [title, classification, file]
      properties:
        title: { type: string, minLength: 1, maxLength: 300 }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        file:
          type: string
          format: binary
    KnowledgeUploadResult:
      type: object
      additionalProperties: false
      required: [document_id, version_id, version_number, ingestion_state, idempotent]
      properties:
        document_id: { type: string, format: uuid }
        version_id: { type: string, format: uuid }
        version_number: { type: integer, minimum: 1 }
        ingestion_state: { type: string, enum: [uploading, queued, processing, indexed, failed] }
        idempotent: { type: boolean }
    KnowledgeAdminSnapshot:
      type: object
      additionalProperties: false
      required: [roles, documents, members]
      properties:
        roles:
          type: array
          uniqueItems: true
          items: { type: string, enum: [knowledge_admin, platform_admin] }
        documents:
          type: array
          items: { "$ref": "#/components/schemas/KnowledgeDocument" }
        members:
          type: array
          items: { "$ref": "#/components/schemas/KnowledgeMember" }
    KnowledgeDocument:
      type: object
      additionalProperties: false
      required: [id, title, classification, status, grant_count, created_at]
      properties:
        id: { type: string, format: uuid }
        title: { type: string, minLength: 1, maxLength: 300 }
        classification: { type: string, enum: [public, internal, confidential, restricted] }
        status: { type: string, enum: [active, deleted] }
        current_version_id: { type: string, format: uuid }
        current_version: { type: integer, minimum: 1 }
        current_ingestion_state: { type: string }
        latest_version_id: { type: string, format: uuid }
        latest_version: { type: integer, minimum: 1 }
        latest_ingestion_state: { type: string }
        latest_failure_code: { type: string }
        latest_failure_detail: { type: string }
        grant_count: { type: integer, minimum: 0 }
        created_at: { type: string, format: date-time }
    KnowledgeVersion:
      type: object
      additionalProperties: false
      required: [id, version_number, checksum, status, ingestion_state, created_at]
      properties:
        id: { type: string, format: uuid }
        version_number: { type: integer, minimum: 1 }
        checksum: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        status: { type: string, enum: [draft, published, superseded] }
        ingestion_state: { type: string, enum: [legacy_indexed, uploading, queued, processing, indexed, failed] }
        failure_code: { type: string }
        failure_detail: { type: string }
        source_format: { type: string, enum: [markdown, text, pdf, docx] }
        original_filename: { type: string }
        size_bytes: { type: integer, minimum: 1, maximum: 33554432 }
        job_state: { type: string, enum: [queued, leased, retryable, succeeded, failed] }
        attempts: { type: integer, minimum: 0, maximum: 8 }
        created_at: { type: string, format: date-time }
        published_at: { type: string, format: date-time }
    KnowledgeMember:
      type: object
      additionalProperties: false
      required: [id, display_name, status, granted]
      properties:
        id: { type: string, format: uuid }
        display_name: { type: string, minLength: 1 }
        status: { type: string, enum: [active, disabled] }
        granted: { type: boolean }
    AgentIntent:
      type: object
      additionalProperties: false
      required: [intent_id, action_type, title, payload_digest, state, expires_at]
      properties:
        intent_id: { type: string, minLength: 1 }
        action_type: { type: string, const: create_ticket }
        title: { type: string, minLength: 1, maxLength: 200 }
        payload_digest: { type: string, pattern: "^sha256:[0-9a-f]{64}$" }
        state: { type: string, enum: [pending_approval, approved, executing, succeeded, failed, expired] }
        expires_at: { type: string, format: date-time }
        execution_id: { type: string }
        execution_state: { type: string, enum: [queued, running, unknown, succeeded, failed] }
        ticket_id: { type: string }
    TelegramLinkStatus:
      oneOf:
        - type: object
          additionalProperties: false
          required: [state]
          properties:
            state: { type: string, const: unbound }
        - type: object
          additionalProperties: false
          required: [state, expires_at]
          properties:
            state: { type: string, const: pending }
            expires_at: { type: string, format: date-time }
        - type: object
          additionalProperties: false
          required: [state]
          properties:
            state: { type: string, const: bound }
    TelegramLinkChallenge:
      type: object
      additionalProperties: false
      required: [state, code, command, expires_at]
      properties:
        state: { type: string, const: pending }
        code:
          type: string
          pattern: "^(?:[A-Z2-7]{4}-){7}[A-Z2-7]{4}$"
        command:
          type: string
          pattern: "^/link (?:[A-Z2-7]{4}-){7}[A-Z2-7]{4}$"
        expires_at: { type: string, format: date-time }
    Error:
      type: object
      additionalProperties: false
      required: [code, message, retryable, correlation_id]
      properties:
        code:
          type: string
          minLength: 1
        message:
          type: string
          minLength: 1
        retryable:
          type: boolean
        correlation_id:
          type: string
          minLength: 1
